'''
Print the largest number of a list
'''

def largest_num_list(ele):

    max = ele[0]

    for i in range (len(ele)):
        ele[i]
        if(ele[i]>max):
            max = ele[i]

    print("The largest number in the list is:", max)
